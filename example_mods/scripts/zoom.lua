function onBeatHit() 
	-- if (curBeat % 4 == 0) then
	-- setProperty('camGame.zoom', getProperty('defaultCamZoom') + 0.15)
	-- setProperty('camHUD.zoom', getProperty('camHUD.zoom') + 0.015)
	-- end
end